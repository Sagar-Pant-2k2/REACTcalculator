import styled from 'styled-components'

const Button = styled.div`
font-size: 24px;
display: flex;
align-items: center;
justify-content: center;
    background: linear-gradient(to top right,black,teal);
`

export default Button;
